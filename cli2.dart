double calculateIMC(double altura, double peso) {
  double peso = 0;
  double altura = 0;
  double resultIMC = peso / (altura * altura);
  // double calculoIMC(double peso, double altura) => peso / (altura * altura);
  // double resultIMC = calculoIMC(peso, altura);
  return resultIMC;
}
